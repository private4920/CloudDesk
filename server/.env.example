# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# JWT Configuration
# Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# PostgreSQL Database
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@host:5432/database

# Firebase Admin SDK Configuration
# Get these from Firebase Console > Project Settings > Service Accounts
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# GCP Compute Engine Configuration
# Set to 'true' to enable real GCP VM provisioning, 'false' for demo mode
GCP_ENABLED=false
# Your GCP project ID (required when GCP_ENABLED=true)
GCP_PROJECT_ID=your-gcp-project-id
# Optional: Timeout for GCP operations in milliseconds (default: 300000 = 5 minutes)
GCP_TIMEOUT_MS=300000

# ============================================
# WebAuthn / Passkey Authentication Configuration
# ============================================
# These variables configure the WebAuthn (passkey) authentication system.
# Passkeys provide phishing-resistant, passwordless authentication using
# biometrics (Face ID, Touch ID, Windows Hello) or security keys (YubiKey).

# Relying Party ID (RP_ID)
# - MUST match your domain name (without protocol or port)
# - For development: use 'localhost'
# - For production: use your domain (e.g., 'example.com' or 'app.example.com')
# - Cannot include protocol (http://, https://) or port numbers
# - Subdomains are allowed (e.g., 'app.example.com')
# - CRITICAL: Changing this value will invalidate all existing passkeys
RP_ID=localhost

# Relying Party Name (RP_NAME)
# - Human-readable name displayed to users during passkey enrollment
# - Shown in browser prompts and authenticator interfaces
# - Should be your application or company name
# - Example: "CloudDesk", "Acme Corp", "My Application"
RP_NAME=CloudDesk

# Expected Origin (ORIGIN)
# - The complete URL where users access your application
# - MUST include protocol (http:// or https://) and port if non-standard
# - For development: http://localhost:5173 (or your dev server port)
# - For production: https://yourdomain.com (always use HTTPS in production)
# - MUST match exactly what appears in the browser address bar
# - Used to prevent cross-origin attacks
# Examples:
#   Development: http://localhost:5173
#   Production:  https://app.example.com
#   Production:  https://example.com
ORIGIN=http://localhost:5173

# ============================================
# WebAuthn Configuration Examples
# ============================================
#
# Local Development:
#   RP_ID=localhost
#   RP_NAME=CloudDesk Dev
#   ORIGIN=http://localhost:5173
#
# Production (root domain):
#   RP_ID=example.com
#   RP_NAME=CloudDesk
#   ORIGIN=https://example.com
#
# Production (subdomain):
#   RP_ID=app.example.com
#   RP_NAME=CloudDesk
#   ORIGIN=https://app.example.com
#
# IMPORTANT NOTES:
# - All three values must be consistent with each other
# - Production MUST use HTTPS (WebAuthn requires secure context)
# - Changing RP_ID invalidates all existing passkeys
# - Test thoroughly in development before deploying to production
